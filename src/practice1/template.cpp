#include <bits/stdc++.h>
#include "log.hpp"
using namespace std;

const int maxn = 2e5;
int n, a[maxn];

void solve() {
    cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];

    

    return;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    freopen("input.txt","r",stdin);

    solve();
    return 0;
}